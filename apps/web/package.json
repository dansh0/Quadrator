{
  "name": "@quadrator/web",
  "version": "0.2.0",
  "private": true,
  "description": "Quadrator web shell: the shared UI on BrowserPlatformAdapter, built as a static site.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "typecheck": "vue-tsc --noEmit",
    "e2e": "playwright test"
  },
  "dependencies": {
    "@quadrator/ui": "workspace:*"
  },
  "devDependencies": {
    "@playwright/test": "^1.58.2",
    "@vitejs/plugin-vue": "^6.0.7",
    "typescript": "^5.9.3",
    "vite": "^8.1.3",
    "vue-tsc": "^3.3.6"
  }
}
